name: Deploy image to Elastic Beanstalk
description: >
  Points a single-container Docker Elastic Beanstalk environment at an
  already-built, already-pushed ECR image via a generated Dockerrun.aws.json.
  Beanstalk never builds anything itself here — it only runs the image
  build-push-ecr already produced.

inputs:
  application-name:
    required: true
  environment-name:
    required: true
  aws-region:
    required: true
  image-uri:
    description: Output of the build-push-ecr action (registry/repo:tag)
    required: true
  container-port:
    required: true
  version-label:
    description: Unique label for this deploy, shown in the Beanstalk console's version history
    required: true
  nginx-proxy-timeout:
    description: >
      Seconds Beanstalk's nginx will wait for the container before returning
      504. Beanstalk's default is 60s, which is fine for a web request but not
      for work measured in minutes — scraping a source or embedding a batch
      both exceed it, and the caller sees "status code 504" with no clue that
      a proxy, not the app, gave up. Leave unset to keep the default.
    required: false
    default: ""
  nginx-client-max-body-size:
    description: >
      Value for Beanstalk nginx client_max_body_size. EB default is 1M — any
      upload larger than that (e.g. 5.4 MB PDF to POST /documents) gets a 413
      Content Too Large from nginx before reaching Node/Python, regardless of
      the app's own MAX_FILE_SIZE/Multer limit (20 MB). Set to 100M to cover
      the app's maximum plan limit and AI's 100 MB ceiling. Overridden per-deploy
      if needed; the repo's static .platform file is a fallback for local/zip.
    required: false
    default: "100M"
  nginx-client-body-buffer-size:
    description: >
      Value for client_body_buffer_size paired with client_max_body_size.
    required: false
    default: "16k"

runs:
  using: composite
  steps:
    - name: Write Dockerrun.aws.json
      shell: bash
      run: |
        cat > Dockerrun.aws.json <<EOF
        {
          "AWSEBDockerrunVersion": "1",
          "Image": {
            "Name": "${{ inputs.image-uri }}",
            "Update": "true"
          },
          "Ports": [{ "ContainerPort": "${{ inputs.container-port }}" }]
        }
        EOF

        # .platform/nginx/conf.d/*.conf is merged into Beanstalk's own nginx
        # config at deploy time — the only supported way to override nginx
        # settings on the Docker platform.
        #
        # Two overrides are managed here:
        # 1) client_max_body_size — EB nginx defaults to 1M, so any upload
        #    larger than that (e.g. 5.4 MB PDF to POST /documents) gets a 413
        #    from nginx before reaching Node/Python, regardless of the app's
        #    own MAX_FILE_SIZE_MB (20 MB default, up to 100 MB for MAX plan).
        #    This is written unconditionally so every deploy fixes 413 without
        #    requiring a caller to remember an extra input.
        # 2) proxy timeouts — only when nginx-proxy-timeout is set, for
        #    long-running routes (scrape, embed) that exceed the 60s default.
        mkdir -p .platform/nginx/conf.d

        # Always include upload-size override (action default 100M covers API 20M
        # + AI 100M ceilings; repo fallback is .platform/.../client_max_body_size.conf).
        cat > .platform/nginx/conf.d/client_max_body_size.conf <<EOF
        client_max_body_size ${{ inputs.nginx-client-max-body-size }};
        client_body_buffer_size ${{ inputs.nginx-client-body-buffer-size }};
        EOF
        echo "nginx client_max_body_size set to ${{ inputs.nginx-client-max-body-size }} (buffer ${{ inputs.nginx-client-body-buffer-size }})"

        TIMEOUT="${{ inputs.nginx-proxy-timeout }}"
        if [ -n "$TIMEOUT" ]; then
          cat > .platform/nginx/conf.d/proxy_timeout.conf <<EOF
        proxy_connect_timeout ${TIMEOUT}s;
        proxy_send_timeout    ${TIMEOUT}s;
        proxy_read_timeout    ${TIMEOUT}s;
        EOF
          echo "nginx proxy timeout set to ${TIMEOUT}s"
        else
          # If the caller didn't request a timeout override but the repo ships
          # a default (proxy_timeout_default.conf), keep it in the zip — don't
          # delete it. Remove only a stale proxy_timeout.conf from a previous
          # manual run in this workspace.
          if [ -f .platform/nginx/conf.d/proxy_timeout.conf ]; then
            rm -f .platform/nginx/conf.d/proxy_timeout.conf
            # Re-check for repo default after removal
            if [ ! -f .platform/nginx/conf.d/proxy_timeout_default.conf ]; then
              echo "no nginx proxy timeout override (60s default)"
            fi
          fi
          if [ -f .platform/nginx/conf.d/proxy_timeout_default.conf ]; then
            echo "using repo proxy timeout default (proxy_timeout_default.conf)"
          fi
        fi

        # Always zip Dockerrun + .platform — .platform now at minimum contains
        # client_max_body_size.conf, so every deploy carries the 413 fix.
        zip -r deploy.zip Dockerrun.aws.json .platform
        echo "deploy.zip contents:"
        unzip -l deploy.zip | head -n 40

    # Beanstalk rejects a deploy outright — 400 InvalidParameterValue,
    # "Environment ... is in an invalid state for this operation. Must be
    # Ready." — whenever the environment is mid-change: a previous deploy, an
    # instance replacement, or a console/CLI configuration update (e.g.
    # changing environment properties or the root volume size). The deploy
    # step below only waits for recovery *after* it has started, so without
    # this the job dies in seconds on a race it could simply have waited out.
    #
    # Credentials come from the OIDC step inside build-push-ecr, which runs
    # earlier in the same job and exports them to the job environment.
    - name: Wait for environment to be Ready
      shell: bash
      run: |
        status=unknown
        for attempt in $(seq 1 80); do
          status=$(aws elasticbeanstalk describe-environments \
            --application-name "${{ inputs.application-name }}" \
            --environment-names "${{ inputs.environment-name }}" \
            --region "${{ inputs.aws-region }}" \
            --query "Environments[0].Status" --output text 2>/dev/null || echo "Unknown")
          if [ "$status" = "Ready" ]; then
            echo "Environment is Ready (after $attempt check(s))."
            exit 0
          fi
          echo "Environment status is '$status' — waiting 15s ($attempt/80)…"
          sleep 15
        done
        echo "::error::${{ inputs.environment-name }} never reached Ready within 20 minutes (last status: $status)."
        exit 1

    - name: Deploy
      uses: einaregilsson/beanstalk-deploy@v22
      with:
        # This action only reads credentials from these inputs when run as a
        # GitHub Action — unlike its CLI mode, it does NOT fall back to the
        # ambient AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY env vars that
        # build-push-ecr's OIDC step already exported via GITHUB_ENV, so they
        # have to be forwarded explicitly here.
        aws_access_key: ${{ env.AWS_ACCESS_KEY_ID }}
        aws_secret_key: ${{ env.AWS_SECRET_ACCESS_KEY }}
        aws_session_token: ${{ env.AWS_SESSION_TOKEN }}
        application_name: ${{ inputs.application-name }}
        environment_name: ${{ inputs.environment-name }}
        region: ${{ inputs.aws-region }}
        version_label: ${{ inputs.version-label }}
        deployment_package: deploy.zip
        wait_for_environment_recovery: 300
        # version-label is derived from run_number+sha (see ci-cd.yml), so a
        # re-run of the same workflow run reuses the same label. Without
        # this, a retry after a failed deploy always errors with "Version
        # ... already exists" even though nothing about the build changed.
        use_existing_version_if_available: true
